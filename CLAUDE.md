# condo-voice

หุ่นยนต์ต้อนรับพูดภาษาไทยประจำห้องขายคอนโด "Embassy World" (Empire Group)
ใช้ Gemini Live API แบบ speech-to-speech คุมสไลด์บนหน้าต่าง Canva จริง

**คุยกับผู้ใช้เป็นภาษาไทย** เขาสื่อสารภาษาไทย และชอบคำตอบสั้น ตรงประเด็น

---

## สองบุคลิกในรีโปเดียว

`ASSISTANT_PROFILE` ใน `.env`: `condo` (default — พนักงานต้อนรับห้องขาย) หรือ
`emma` (ผู้ช่วยส่วนตัวของเจ้าของ ตาม `docs/jarvis-roadmap.md`) จงใจไม่ fork
เพราะบั๊กที่แก้แล้วยี่สิบกว่าตัวต้องไม่ถูกแก้สองรอบ กติกาของรอยต่อ:
**default ต้องเป็น condo เสมอ** (เครื่องห้องขาย pull แล้วห้ามเปลี่ยนพฤติกรรม),
profile สะกดผิดได้ condo ไม่ใช่ error, emma ไม่ได้ facts/สไลด์/เครื่องมือของ
ห้องขาย (TOOL_GROUPS ว่าง = smarthome อย่างเดียว) แต่**เก็บกฎที่จ่ายด้วยบั๊กจริง
ทุกข้อ** — ฟังไม่ชัดห้ามเดา, ทวนตัวเลข, อ่านผล hardware ก่อนยืนยัน, ห้ามอ่าน
markdown เทสต์คุมอยู่ใน `tests/test_profiles.py`

**Wake word "Emma"** (`WAKE_ENABLED`, ปิดเป็น default): sherpa-onnx KWS ใน
`app/wake.py` + `/ws/wake` ใน main.py ฟังในเครื่องล้วนๆ Gemini เปิดหลังได้ยินชื่อ
เท่านั้น — นี่คือกลไกที่ทำให้เปิดหน้าเว็บทิ้งไว้ทั้งวันไม่เผา quota
เลือก sherpa-onnx แทน openwakeword เพราะรับคำปลุกเป็น*ข้อความ* (เปลี่ยนชื่อ =
แก้ .env) ส่วน openwakeword มีแต่โมเดลสำเร็จรูป (hey_jarvis) ไม่มี emma ต้องเทรนเอง
ข้อจริงที่วัดแล้วและห้ามเขียนสวนทาง: `reset_stream` หลัง hit **ไม่ใช่**ตัวกัน
ยิงซ้ำ (`get_result` เคลียร์ตัวเองต่อ utterance — วัดแล้วทั้งสองแบบเหมือนกัน)
เก็บไว้ตามตัวอย่าง upstream เฉยๆ / เทสต์ detection ใช้ wav สังเคราะห์จาก TTS
ที่ commit ไว้ใน tests/data/wake/ — เครื่องไหนก็รันได้ ไม่ติดกับดัก "ผ่านเพราะ
เครื่องขาดของ" / โมเดลอยู่ data/wake/ (gitignore, คืนได้ด้วยคำสั่งเดียว)

## กฎเนื้อหาที่ห้ามละเมิด

**ห้ามแต่งข้อมูลโครงการเองเด็ดขาด** โดยเฉพาะราคา โปรโมชั่น แบบห้อง ขนาด เวลาทำการ
เจ้าของโปรเจกต์บอกไว้ตรงๆ ว่า *"ไม่มีในเอกสารพรีเซนต์ ผมไม่กล้าแต่งเอง"*
สี่ฟิลด์นี้ยังว่างใน `data/condo_facts.json` และต้องว่างจนกว่าเขาจะกรอกเอง
ถ้าหุ่นยนต์ถูกถาม มันต้องบอกตรงๆ ว่าไม่มีข้อมูลแล้วให้ติดต่อฝ่ายขาย

**สคริปต์บรรยายมีสถานะการอนุมัติ** `script_approved` แยก draft ออกจาก copy ที่คนเซ็นรับรองแล้ว
ทั้ง 59 หน้าตอนนี้เป็น draft (ร่างจากภาพสไลด์ ไม่มีใครตรวจ)
เคยมีบั๊กที่ flag เป็น approved ทั้งหมด — กลไกความปลอดภัยกลับหัว ระวังอย่าให้เกิดซ้ำ
อนุมัติด้วย `python scripts/approve_narration.py --all --by "ชื่อ"`

---

## คำสั่ง

```bash
uvicorn app.main:app --port 8000     # รันเซิร์ฟเวอร์
python -m pytest tests/ -q            # เทสต์ (566 ตัว, ใช้เวลา ~3 นาที)
python -m pytest tests/test_slides.py -q
python scripts/eval_search.py         # วัดคุณภาพการค้นสไลด์
python scripts/analyze_log.py --days 7  # อ่าน data/logs/ ว่าเกิดอะไรขึ้นจริง
python scripts/approve_narration.py --all --by "ชื่อ"
python scripts/build_embeddings.py    # สร้าง embeddings ครั้งเดียว (อดทนกับ rate limit)
python scripts/fetch_wake_model.py    # โหลดโมเดล wake word ครั้งเดียว (~15MB)
python scripts/make_test_documents.py # สร้าง PDF ทดสอบระบบพิมพ์ (ต้องรันบนเครื่องที่มีฟอนต์ไทย)
```

เทสต์ทั้งชุดใช้เวลานานกว่า 45 วินาที ถ้า shell มี timeout ให้แบ่งรันทีละไฟล์

**เจ้าของโปรเจกต์รันบน Windows PowerShell** ไม่มี `head`, `grep`, `tail`
อย่าให้คำสั่งที่มี pipe แบบ Unix — ใช้ `Select-Object -First 20`,
`Select-String` แทน หรือให้รันเปล่าๆ ไปเลย

---

## โครงสร้าง

```
app/
  session.py          WebSocket loop, ตัวกระตุ้นทัวร์, ตัวตามหน้า Canva
  display.py          คิวเสียง + การเผยสไลด์ (audio lead อยู่ที่นี่)
  prompts.py          system instruction (facts มาจาก data/condo_facts.json)
  config.py           ทุก setting อ่านจาก .env
  providers/          gemini.py (หลัก), openai_realtime.py
  tools/
    registry.py       @tool decorator, timeout, dispatch
    slides.py         ทัวร์ สถานะ คำสั่งที่ส่งไปกับ tool result
    canva_display.py  ขับ Canva จริงผ่าน Playwright
    slide_search.py   BM25 + embeddings + RRF
    retrieval.py      tokenizer, BM25, semantic index
    knowledge.py      search_condo_info
    robot.py          เดิน/หยุด/กลับฐาน (Aobo SDK)
    robot_link.py     ส่งคำสั่งลง WebSocket ให้แอปบนหุ่น + mock
  turnlog.py          เขียน jsonl หนึ่งบรรทัดต่อเทิร์น (data/logs/)
data/slides/index.json  สไลด์ 144 (เป็นเด็ค 59) + script_th + ask_th
data/condo_facts.json   ราคา/โปรโมชั่น/ทำเล + approved_by + effective_from
data/logs/*.jsonl       log เส้นทางเสียง (ไม่ commit — มีคำพูดลูกค้า, ลบเองใน 30 วัน)
data/log-summaries/     สรุปเป็นตัวเลข ไม่มีคำพูดใคร เก็บถาวรได้ (analyze_log --save)
docs/                   คู่มือสำหรับทีมขาย (ภาษาไทย)
```

---

## สิ่งที่ต้องรู้ก่อนแก้ (แต่ละข้อคือบั๊กที่เคยเกิดจริง)

### เสียงกับภาพ

**โมเดลสร้างข้อความเร็วกว่าพูด 4-6 เท่า** ทุกอย่างที่เปลี่ยนสิ่งที่ลูกค้า*มองเห็น*
ต้องเช็ค `display.remaining_lead()` ก่อน ไม่งั้นภาพแซงเสียง
เคยมีสองเส้นทางสั่ง Canva ที่ใช้นาฬิกาคนละเรือน — Canva กระโดดล่วงหน้า จอเว็บอยู่กับที่

**ยกเว้นสไลด์แรกของทัวร์** (`hold=True`) ตอนนั้นยังไม่มีหน้าไหนให้ "เร็วเกิน"
และ Chromium ยังเปิดไม่เสร็จ ต้องรอจริง — สังเกตว่า lead ตอนนั้น**ไม่**เป็นศูนย์
เพราะเพิ่งพูดคำทักทายไป การเช็ค lead เฉยๆ จะข้ามเคสที่มันถูกเขียนมาเพื่อแก้พอดี

**`SLIDE_TOOL_BUDGET_S` คือระยะที่ภาพนำเสียงได้** ไม่ใช่แค่ตัวเลข timeout
สคริปต์กลางๆ พูด ~10 วิ ยาวสุด ~30 วิ ถ้า budget สั้นกว่านั้น ครึ่งเด็คจะเลื่อนก่อนคนฟังทัน
`next_slide` ไม่ใช่เครื่องมือช้า มันตั้งใจรอ เลยมี `paced=True` และเพดานแยก

### คำสั่งกับโมเดล

**คำสั่งที่ติดไปกับ tool result ถึงโมเดลจริง คำสั่งท้าย system prompt ไม่ถึง**
กฎเดียวกันเป๊ะ วางท้าย prompt = ถูกเมิน ติดกับ tool result = ทำตาม
`KEEP_GOING`, `SPEAK_SCRIPT`, `ASK_AND_WAIT`, `FOLLOW_PAGE` ใน `slides.py` คือกลไกนี้

**อ้างคำที่โมเดลพูดผิดจริงๆ ลงไปในคำสั่ง** "ห้ามเกริ่นล่วงหน้า" ไม่ได้ผล
`"ห้ามลงท้ายว่า 'เดี๋ยวไปดู...กันต่อนะคะ'"` ได้ผล เพราะมันจับคู่กับสิ่งที่กำลังจะพิมพ์ออกมา

**ระวังคำสั่งที่ขัดกันเอง** เคยเจอสองรอบ:
"พูดเฉพาะในฟิลด์ script" (ไทย) ปะทะ "ตอบภาษาเดียวกับลูกค้า" — ต้องระบุว่าแปลได้
"ห้ามตอบเรื่องอื่นทุกกรณี" ปะทะการที่หุ่นยนต์เป็นฝ่ายถามลูกค้าก่อน — ต้องเปิดช่องไว้

**Gemini 3.1 ไม่รองรับ** asynchronous function calling (NON_BLOCKING), proactive audio,
affective dialogue — ยืนยันจากเอกสาร migration ของ Google
`GEMINI_AFFECTIVE_DIALOG=true` ใน `.env` ตอนนี้ถูกเมินและมี warning ขึ้น log

### ตัวกระตุ้นทัวร์

**อย่าให้ตัวกระตุ้นป้อนตัวเอง** `CONTINUE_NUDGE` ลงท้ายว่า "อย่าพูดอย่างอื่น"
โมเดลเชื่อฟัง: เรียก next_slide แล้วเงียบ จบเทิร์น → เทิร์นเงียบดูเหมือนค้าง → กระตุ้นซ้ำ
เด็คเดินเอง 26 หน้าโดยไม่พูดสักคำ `MAX_TOUR_NUDGES` กันไม่อยู่เพราะนับซ้ำที่ index เดิม
แต่ทุกครั้งที่กระตุ้น index ก็ขยับ ตัวนับรีเซ็ตทุกรอบ
**ขอบเขตที่สิ่งที่ถูกจำกัดรีเซ็ตได้เอง ไม่ใช่ขอบเขต**

**ตัวกระตุ้นต้องรอให้ลูกค้าฟังจบก่อน** `turn_complete` แปลว่าโมเดลหยุด*สร้าง* ไม่ใช่มีคนได้ยิน
ตอนนั้นอาจมีเสียงค้างในคิวอีก 20 วิ การส่งคำกระตุ้นเข้าไปคือเทิร์นของ user
ซึ่ง Gemini อ่านว่าถูกพูดแทรก แล้วทิ้งเสียงที่เหลือ — วัดจริงได้ 21 จาก 81 สไลด์
พูดไม่ถึง 60% ของบท น้อยสุด 16% กลไกที่สร้างมาให้ทัวร์เดินต่อ กลายเป็นตัวตัดทัวร์เอง
`_nudge_once_heard` รอ `wait_until_heard` ก่อนเสมอ

**และมันรออยู่ที่เดียว ทั้งที่มีสี่ทางที่หุ่นพูดเองได้** ตัวกระตุ้นทัวร์, resume หลังเงียบ,
ตามหน้า Canva, หุ่นถึงที่หมาย — สามในสี่ยิง `send_text` ทันที `robot_link.arrived`
ชัดสุด เพราะหุ่นเดินถึงตอนไหนก็ได้ รวมถึงกลางประโยคที่กำลังอธิบายการเดินนั้นอยู่
= หุ่นพูดแทรกตัวเองเพื่อบอกว่ามาถึงแล้ว
ตอนนี้ทุกทางผ่าน `events.announce()` ทางเดียว ซึ่งทำสามอย่างที่แต่ละทางเคยลืมคนละอย่าง:
รอคิวเสียง, เช็ค `still_relevant()` อีกครั้ง**หลัง**รอ (สถานการณ์เปลี่ยนได้ระหว่างนั้น),
และล็อกไม่ให้สองเหตุการณ์ยิงพร้อมกัน — ข้อหลังยังไม่เคยเกิด เพราะเมื่อก่อนมีแหล่งเดียว
พอมีกล้อง+หุ่น+นาฬิกาแล้วจะเกิดแน่ และมันคือบั๊กพูดแทรกอันเดิมโดยมีหุ่นเป็นคนแทรกเอง
`announce` ยังจับเซสชันไว้**ก่อน**รอ แล้วเช็คซ้ำตอนจะส่ง ถ้าระหว่างนั้นมีคนเปิดแท็บใหม่
ข่าวของลูกค้าคนเก่าต้องไม่ไปโผล่กับคนใหม่

**เทสต์ที่ assert ว่า "ไม่ได้ส่งอะไร" ต้องลงทะเบียน `session._active` ด้วย** ไม่งั้น
`announce` ทิ้งข้อความตั้งแต่แรกเพราะไม่มีเซสชัน แล้วเทสต์ผ่านโดยไม่ได้ตรวจอะไรเลย
`_lone_session()` ตั้งให้แล้ว และ conftest ล้างให้ทั้งก่อนและหลัง

**ถ้าหยุดกระตุ้น ต้องมีอะไรมาปลุก** ตอนหุ่นยนต์รอคำตอบ มันจะไม่มีเทิร์นใหม่อีกเลย
(ไม่มีเสียง ไม่มี tool call) `_resume_after_silence` ใน `session.py` มีนาฬิกาแยกด้วยเหตุนี้

**เด็คที่จบแล้วต้องเลิกเป็นเด็ค** `next_slide` เคยคืน `finished` แต่ทิ้ง deck ไว้ใน STATE
ต่อมาลูกค้าพูด "ขอดูฟิตเนส" → `show_slide` ตั้ง `detour` → `should_continue_tour()`
เห็น detour แล้วตอบว่าใช่ → ตัวกระตุ้นยิง → บรรยายสไลด์ 59 ทับสิ่งที่ลูกค้าเพิ่งขอ
ตำแหน่งมีค่าเฉพาะตอนยังมีสไลด์ค้างอยู่ พอจบเด็คก็ไม่มี

**`go_to_page` ห้ามส่ง `KEEP_GOING`** เพราะมันลงท้ายว่า "พูดจบแล้วเรียก next_slide ทันที"
ซึ่งเป็นคำสั่งของ*ทัวร์* ติดไปกับคำตอบเรื่องหน้าเดียว = "อธิบายสไลด์หน้า 50"
กลายเป็นพรีเซนต์ตั้งแต่หน้า 50 ต่อไปเรื่อยๆ ใช้ `SPEAK_SCRIPT` แทน
และตั้ง `STATE["manual"]` เพื่อกันตัวกระตุ้น — `next_slide` ที่ถูกสั่งตรงๆ จะปลดล็อกเอง

**ปิดหน้าต่างแล้วอย่าปลุกมันขึ้นมาโชว์หน้าดำ** `close_presentation` ปิดเบราว์เซอร์
แล้ว `display.show(None)` → `goto(None)` → `_sync` เรียก `_ensure_page()` เปิด Chromium
ใหม่ทั้งตัวเพื่อโชว์ `data:text/html,...` สีดำ — ลูกค้าสั่งปิดแล้วเห็นหน้าต่างใหม่เด้งมา

**และครั้งแรกแก้ไม่หมด — ตรงนี้คือบทเรียนหลัก** ด่านที่ใส่ไว้คือ
`if target == BLANK and _page is None` ซึ่งครอบแค่ "ไม่มีหน้าต่าง" ส่วน handle ที่*ตายแล้ว*
ไปเข้าอีกกิ่งหนึ่ง (`_is_dead` → `_shutdown_quietly` → เปิดใหม่) วันที่ 2026-08-10 log เขียนว่า
`close_presentation` 09:15:09 แล้ว `canva_launch` 09:15:10 `canva_fullscreen` 09:15:13
ตอนนี้ `_ensure_page(launch=...)` รับ*เจตนาของผู้เรียก* แทนการเดาจาก module state
และ `canva_launch` มีฟิลด์ `reason` แล้ว เพราะครั้งนี้ใช้เวลาไล่ว่า "โค้ดเส้นไหนเปิด"
นานกว่าตอนแก้จริง

**ห้ามเทสต์ "ไม่เปิดหน้าต่าง" ด้วยการ patch `_ensure_page`** เทสต์แบบนั้นยืนยันได้แค่ด่านเดียว
ที่ตัวเองรู้จัก — และเป็นเหตุผลที่ทางเข้าอีกทางรอดมาได้ ต้อง patch `_launch_browser`
และต้องปลอม `playwright` ใน `sys.modules` ด้วย (`_pretend_playwright_is_installed`)
ไม่งั้นเทสต์ผ่านเพราะเครื่อง CI ไม่มี playwright ไม่ใช่เพราะโค้ดถูก — ลอง revert แล้วยังเขียว

**กระโดดไกลใน Canva ห้ามใช้ `page.goto`** เกิน `MAX_STEPS` โค้ดเก่าตกไปที่ goto
ซึ่งเป็นการ navigate เต็มรูปแบบ Canva รื้อเด็คแล้วสร้างใหม่ ~3.5 วิ จอว่าง
ตอนทัวร์ไม่เห็นปัญหาเพราะไปทีละหน้า แต่ "ขอดูฟิตเนส" จากหน้า 59 คือจอดำก่อนเห็นภาพ
`_jump_to` เขียน `location.hash` แทน — viewer เป็น SPA จึงย้ายเองโดยไม่ reload

**แยก "ถูกพูดแทรก" ออกจาก "พูดจบแล้วค่อยถาม"** สองเคสนี้ทิ้ง detour เหมือนกัน
แต่เคสแรกยังค้างเนื้อหา เคสหลังฟังจบแล้ว — เล่าซ้ำคือหุ่นยนต์ไม่ทันสังเกต
`STATE["unheard"]` ตั้งเฉพาะใน `pause_for_barge_in()` เท่านั้น

### สภาพที่ค้างข้ามเซสชัน

**`go_away` ไม่ใช่คำเตือน แต่เป็นคำสั่งให้ปิดเอง** Gemini ส่งมาก่อนหมดเวลา session
ถ้าไม่ปิดแล้วรีคอนเนกต์ทันที เซิร์ฟเวอร์จะฆ่าด้วย 1008 policy violation
แล้ว `send_audio` จะโยน exception เข้า `_browser_to_provider` ซึ่งทำให้
`asyncio.wait(FIRST_COMPLETED)` จบทั้งเซสชัน — การหมุนเวียนที่กู้ได้กลายเป็นหุ่นยนต์ตาย

**resume แล้วต้องปลุกทัวร์เอง** แก้ go_away ไม่ให้ตายแล้วได้ "ค้าง" มาแทน:
โมเดลที่ resume กลับมาแล้วตื่นแต่*ว่าง* ไม่มีเทิร์นให้จบ ไม่มีเหตุให้พูด ทุกเส้นทาง
ที่ปลุกทัวร์ผูกกับ `turn_complete` ซึ่งข้าม reconnect ไปแล้วไม่มี → ทัวร์หยุดนิ่ง
provider ยิง `ProviderEvent(kind="resumed")` ให้ session รู้

**`--kiosk` ใช้ได้เฉพาะกับหน้าต่างแรกของเบราว์เซอร์เท่านั้น** `browser.new_page()`
เปิดหน้าต่าง*ใหม่*ผ่าน CDP แฟลกเลยไปตกกับหน้าต่างเปล่าที่ไม่มีใครดู ส่วนสไลด์
ไปอยู่หน้าต่างธรรมดาที่มีแท็บมีแถบที่อยู่ครบ ต้องใช้ `launch_persistent_context()`
แล้วเอา `pages[0]` — และ `BrowserContext` ไม่มี `is_connected` (มีแต่ `Browser`)
ถ้า `_is_dead` เรียกแล้วถือว่า AttributeError = ตาย จะเปิดหน้าต่างใหม่ทุกสไลด์ไม่จบ

**`canva_display._page` อยู่ระดับ module อายุยืนกว่าเซสชัน** ถ้าหน้าต่างตาย
handle เก่าจะถูกใช้ต่อไปเรื่อยๆ ทุกคำสั่งเป็น fire-and-forget กลืน error หมด
อาการคือ "canva ไม่ขึ้น" โดย log สะอาด และรีสตาร์ต*เซสชัน*ไม่ช่วย เพราะศพอยู่ที่ module

**ลูกค้าต้องสั่งหยุดพรีเซนต์ได้** มี `stop_presentation` เพราะเคยไม่มี —
ลูกค้าพูด "หยุด พรีเซนต์" โมเดลตอบ "ได้ค่ะ หยุดแล้ว" แล้วบรรยายสไลด์ถัดไปต่อ
เพราะพูดคือทั้งหมดที่มันทำได้ `hide_slide` ล้างเด็คได้แต่คำอธิบายพูดถึงการปิดจอ
โมเดลเลยไม่เคยเรียกมันเลยสักครั้งใน 59 สไลด์

**"หยุด" กับ "เลิก" ไม่ใช่เรื่องเดียวกัน — และนี่คือบั๊กที่ตัวแก้อันก่อนสร้างขึ้นมาเอง**
`stop_presentation` เรียก `reset_state()` = ทิ้ง deck/index/tour ทั้งหมด พอ 2026-08-10
ลูกค้าถามว่า "Can you speak Chinese?" กลางสไลด์ 2 โมเดลอ่านว่าเปลี่ยนใจ เรียก stop
ตอบคำถามเป็นจีน แล้ว*จบแค่นั้น* — ทางกลับมีทางเดียวคือ `start_presentation`
ซึ่งเริ่มที่หน้าปก คนที่ดูไป 2 สไลด์แล้วโดนเสนอสไลด์ 1 ใหม่
ตอนนี้ `STATE["parked"]` เก็บตำแหน่งไว้ และมี `resume_presentation`
`parked["unheard"]` ตัดสินว่ากลับมาแล้วเล่าสไลด์เดิมซ้ำ (ถูกพูดแทรก) หรือไปหน้าถัดไป (ฟังจบแล้ว)

**คำถามไม่ใช่คำสั่งให้หยุด** โค้ดกันไม่ได้ โมเดลเป็นคนเลือกเครื่องมือเอง ด่านจึงต้องอยู่ใน
*คำอธิบายเครื่องมือ* และต้องระบุเคสที่เกิดจริงตรงๆ ("ขอเปลี่ยนภาษา") ไม่ใช่บรรยายกว้างๆ
มีเทสต์อ่าน description จริงเพื่อไม่ให้ประโยคนี้หายไปเงียบๆ

### เอกสารกับการพิมพ์

**หุ่นยนต์ไม่สร้างเอกสาร มันเลือกจาก `data/documents/catalogue.json`** ทีมขายเขียนไฟล์
ทีมขายเป็นเจ้าของเนื้อหา — เคยคิดจะให้สร้างใบสัญญาเอง แต่ราคายังว่าง `approved_by` ยังว่าง
และหุ่นยนต์ยืนยันตัวตนคนเซ็นไม่ได้ = ย้ายความรับผิดจากคนไปที่เครื่องที่ฟังผิดได้

**การพิมพ์ PDF บน Windows พังได้สามชั้น เจอครบทั้งสามในวันเดียว**
1. ส่ง path สัมพัทธ์ → `Start-Process` แก้เทียบไดเรกทอรีของ*ตัวเอง* → Windows ฟ้องว่า
   "No application is associated" ซึ่งพูดถึงการผูกไฟล์ แต่ปัญหาจริงคือ path
2. `-Verb Print` ใช้เครื่องพิมพ์เริ่มต้นเสมอ อ่าน `PRINTER_NAME` มาแล้วไม่ได้ใช้ — ต้อง `PrintTo`
3. เครื่องไม่มีแอปผูกกับ `.pdf` เลย → ใช้ `PDF_PRINT_EXE` (SumatraPDF) แทน ไม่พึ่ง association
   `find_pdf_helper()` หา path เองจาก `%LOCALAPPDATA%` เพราะตัวอย่างใน docs เขียน `C:\tools\`
   ซึ่งไม่ใช่ที่ที่ installer ลงจริง และทำให้พังบนเครื่องที่โปรแกรมเปิดค้างอยู่ตรงหน้า

**จำกัดจำนวนชุด** การฟังผิดที่นี่กลายเป็นกระดาษ — ใน log จริงมี "อะไรนะ 50"

### หุ่นยนต์ Astronaut

**เซิร์ฟเวอร์เรียก SDK ไม่ได้** `aoborobotsdk-v2.0.aar` เป็น Android เท่านั้น และ
`quickConnect()` วิ่งไป `192.168.11.1:1445` คือบอร์ดนำทางในตัวหุ่น
แอปบนหุ่นเป็นคนเรียก เซิร์ฟเวอร์ส่งคำสั่งลง **WebSocket เส้นเดิม** ไม่เปิดพอร์ตใหม่
รายละเอียดโปรโตคอลอยู่ใน `docs/ต่อกับหุ่นยนต์ Astronaut.md`

**`go_to_place` ห้ามรอจนถึงที่หมาย** เดินครึ่งนาที + function calling เป็น synchronous
= หุ่นยืนเงียบครึ่งนาทีข้างลูกค้าที่มันเพิ่งบอกว่าจะพาไป บั๊กเดียวกับที่
`start_presentation` เคยรอ Chromium แล้วเงียบ 4 วินาที
การแจ้งว่าถึงกลับมาเป็น**เทิร์นใหม่** (`robot_arrived` → `robot_link.arrived`)
ไม่ใช่ค่าที่คืนจากเครื่องมือ — เก็บไว้ในสถานะเฉยๆ จะไม่มีใครอ่าน

**`available()` ต้องไม่แปลว่า "มีซ็อกเก็ตเปิดอยู่"** เบราว์เซอร์ก็ต่อ `/ws` เหมือนกัน
และมันไม่มีขา ต้องรอ `robot_ready` จากแอปบนหุ่นเท่านั้น

**อย่าใช้ `robust_tokens` จับคู่ชื่อสถานที่** มันผสม character n-gram เข้าไปเพื่อเพิ่ม
recall ของ*การค้นหา* พอเอามาจับ POI แล้ว "ห้องน้ำชั้นสาม" แมตช์ "ห้องตัวอย่าง"
ด้วยคะแนน 3 (`ห้อง`, `ห้อ`, `้อง`) — รูปเดียวกับ `ราคา` แมตช์ `อาคาร` เป๊ะ
ตอนนี้ `find_place` ใช้ `tokenize` (คำล้วน) และบังคับว่า**ทุกคำในชื่อ POI ต้องอยู่ในคำขอ**
สไลด์ผิดแก้ด้วยประโยคเดียว จุดหมายผิดคือลูกค้าเดินข้ามห้องไปแล้ว

**mock ต้องไม่ถูกรายงานเป็น ok** กฎเดียวกับ IR ผลลัพธ์ทุกอันมี `hardware`
และตอน mock คำสั่งบอกโมเดลตรงๆ ว่า "ห้ามบอกว่ากำลังเดินไป" —
เพราะลูกค้ายืนอยู่ข้างๆ และเห็นว่ามันไม่ขยับ

### เทสต์ต้องไม่ทำอะไรกับเครื่องจริง

**pytest ห้ามเปิดหน้าต่างเบราว์เซอร์** วันที่ 2026-08-11 รันเทสต์บน Windows แล้ว
Chrome เต็มจอแบบ kiosk เด้งขึ้นมา โชว์ `DNS_PROBE_FINISHED_NXDOMAIN` ของ `canva.test`
(URL ปลอมจาก fixture ของเทสต์เอง) ไม่มีแถบที่อยู่ ไม่มีปุ่มปิด
ตัวการคือ `test_ensure_page_actually_drops_the_dead_handle` ที่จงใจเรียก `_ensure_page` จริง
พอ fake shutdown ตั้ง `_page = None` โค้ดก็ไหลไปถึงการเปิดเบราว์เซอร์
**บน CI ไม่มี playwright เลยคืน None ตั้งแต่ import — เทสต์ผ่านด้วยเหตุผลที่ไม่เกี่ยวกับสิ่งที่ทดสอบ
และความเสียหายไปโผล่บนเครื่องเดียวที่ไม่มีใครรัน CI**
รูปกลับด้านของ `_pretend_playwright_is_installed` เป๊ะ: ที่นั่นโมดูลที่หายทำให้เทสต์กลวง
ที่นี่ทำให้เทสต์ดูไม่มีพิษภัย
ตอนนี้ `tests/conftest.py` บล็อก `_launch_browser` ทุกตัว ยกเว้นที่ mark `allow_browser_launch`

**สถานะ asyncio ระดับ module รั่วข้ามเทสต์** เทสต์ที่นี่เรียก `asyncio.run()` ตัวละครั้ง
= **แต่ละเทสต์คือ event loop คนละตัว** แต่ `display._lock`, `canva_display._lock`,
`_reveal_task`, `_task` ถูกสร้างตอน import และอยู่ยาวทั้ง session
ล็อกที่ถูกทิ้งไว้ตอน loop ปิดจะค้างเป็น locked ตลอดกาล ปลดไม่ได้เพราะเจ้าของหายไปแล้ว
และตัวที่ร้ายกว่าเพราะดูไม่มีพิษภัยคือ `_audio_until` — มันเป็น deadline ของ
`time.monotonic()` เทสต์ที่ตั้ง lead ยาวแล้วไม่รอให้หมด จะทำให้ `next_slide` ของเทสต์ถัดไป
นั่งรอใน `wait_until_heard` จน `SLIDE_TOOL_BUDGET_S` = 20 วิ ซึ่งเกิน timeout ของ pytest
อาการคือเทสต์ที่ไม่เกี่ยวข้องเลย "ค้าง" และ stack ชี้ไปที่ตัวที่บังเอิญรันถัดมา
`_fresh_async_state` ใน conftest ล้างให้ทุกตัว

### เทสต์ที่ผ่านเพราะเครื่องขาดของ ไม่ใช่เพราะโค้ดถูก

**เจอสี่ครั้งในวันเดียว ทั้งหมดหน้าตาเหมือนกัน** เทสต์เขียวบนเครื่องที่ไม่มีของบางอย่าง
แล้วแดง (หรือพัง) บนเครื่องห้องขายที่มีของครบ — ซึ่งเป็นเครื่องเดียวที่สำคัญ

| ของที่ขาด | อาการบนเครื่องจริง |
|---|---|
| playwright | pytest เปิด Chrome เต็มจอแบบ kiosk ขึ้นมาจริง |
| sentence-transformers | เทสต์ค้างเกิน timeout เพราะ import ใช้เวลานาน |
| เครือข่ายไปหา Google | การค้นหาไม่ปฏิเสธคำถามนอกเรื่อง (บั๊กจริง ไม่ใช่เทสต์เพี้ยน) |
| node | เทสต์ข้ามไปเงียบๆ ปิดบังบั๊ก encoding บน Windows |

**`pytest.skip` อ่านว่า passed ในบรรทัดสรุป** ถ้าเทสต์ตัวเดียวที่ตรวจเรื่องหนึ่งข้ามไป
เรื่องนั้นไม่ได้ถูกตรวจเลย ให้เขียนเหตุผลใน skip ว่าอะไรกำลังไม่ถูกตรวจอยู่

**อย่าให้เทสต์อ่านสภาพเครื่อง** เจอสามตัว: `find_pdf_helper()` ไล่หา SumatraPDF
ใน `%LOCALAPPDATA%` เองแม้ pin ค่า setting แล้ว, fixture ที่ docstring บอกว่า
"no embeddings" แต่ไม่เคยปิด, และเทสต์ที่ pin `pdf_print_exe` อย่างเดียวซึ่งไม่พอ
patch ที่*ฟังก์ชัน* ไม่ใช่ที่ค่า

**subprocess ที่ส่งข้อความไทยต้องระบุ `encoding="utf-8"`** `text=True` เฉยๆ
ถอดรหัสด้วย codepage ของเครื่อง ซึ่งบน Windows ไทยคือ cp874 — ไบต์ UTF-8 จาก
`JSON.stringify` ถอดไม่ออกและโยน exception บน Linux ดีฟอลต์เป็น UTF-8 อยู่แล้ว
จึงไม่มีวันเห็นปัญหานี้

### เกณฑ์ตัดสินการค้นหา

**`found` คือ `similarity >= MIN_SIMILARITY` *หรือ* `coverage >= MIN_COVERAGE`**
วัดทีละค่าจะได้คำตอบผิด — ทั้ง cosine และ standout ต่างสรุปว่า "ไม่มี threshold
ไหนแยกได้" ทั้งที่ต่างอธิบายกฎแค่ครึ่งเดียว พอวัดเป็นคู่แล้วแยกได้
เพราะคำถามจริงที่ cosine ต่ำคือคำถามไทย/อังกฤษที่ BM25 จับได้เต็มอยู่แล้ว
มีแต่คำถามข้ามภาษาที่พึ่ง cosine ล้วน และพวกนั้นคะแนนสูงกว่าขยะ

**`gemini-embedding-001` ไม่เคยใช้ช่วงล่างของตัวเอง** ข้อความมั่วได้ ~0.6 ไม่ใช่ ~0
เป็นคุณสมบัติของโมเดล ไม่ใช่ของเด็ค อย่าตีความ cosine เหมือน "ความเหมือน 0-1"

**ช่องว่างแคบ 0.011** (ขยะสูงสุด 0.649 / ข้ามภาษาต่ำสุด 0.660)
รัน `scripts/eval_search.py` ใหม่ทุกครั้งที่เด็คเปลี่ยน ทางแก้ที่ทนกว่าคือเติม keyword

**`search_condo_info` ตัดสินจาก hit อันดับหนึ่งเท่านั้น** เคยสแกนทุก hit
ซึ่งไม่ตรงกับคอมเมนต์ข้างๆ ที่เขียนเองว่า "rank บอกว่าใกล้สุดคือใบไหน
found บอกว่าใกล้สุดนั้นใกล้พอไหม" — RRF จัดอันดับด้วยคะแนน*ประนีประนอม*
ระหว่าง BM25 กับ cosine ไม่ใช่ด้วย similarity สไลด์อันดับ 8 จึงมี cosine
สูงกว่าอันดับ 1 ได้ และตอบคำถามได้เองทั้งที่ไม่มีใครจัดให้มันอยู่ต้นๆ
นี่คือช่องว่างระหว่าง eval (ดู hits[0]) กับเครื่องมือ (ดูทุกตัว) —
eval รายงานว่าปฏิเสธขยะครบ ขณะที่เทสต์สองตัวยังแดง ทั้งที่ทั้งคู่ถูก

**ชุดคำถามที่ eval วัด ต้องมีคำถามที่เทสต์ยืนยันครบ** เคยวัดชุดหนึ่งแล้วยืนยัน
อีกชุดหนึ่ง eval รายงาน "ปฏิเสธครบ 9 ข้อ" แต่เทสต์ยังแดงสองตัว มีเทสต์กันไว้แล้ว

### โมเดลกับ free tier

**free tier = เนื้อหาถูกใช้ปรับปรุงผลิตภัณฑ์ของ Google** (หน้า pricing เขียนไว้ตรงๆ
`Used to improve our products: Free = Yes / Paid = No`) และเป็นแบบนี้**ทุกโมเดล**
เพราะเป็นเงื่อนไขของ*ระดับบัญชี* ไม่ใช่ของโมเดล — เปลี่ยนโมเดลไม่ช่วย มีแต่เปิด billing
เรื่องนี้ใหญ่กว่าไฟล์ log ในเครื่องมาก เพราะเสียงลูกค้าออกไปข้างนอกอยู่แล้ว

**ราคาถ้าเปิด billing ไม่แพง** `gemini-3.1-flash-live-preview` เสียงเข้า $0.005/นาที
ออก $0.018/นาที — คุย 10 นาทีไม่ถึง 10 บาท

**`-preview` แปลว่าถูกถอนได้** จึงมี `GEMINI_MODEL_FALLBACK` สลับให้เองตอนต่อไม่ติด
แต่**เฉพาะ error ที่บอกว่าโมเดลใช้ไม่ได้** (404 / not found / quota) เท่านั้น
ถ้า fallback ทุก error เน็ตกระตุก 5 วินาทีจะกลายเป็นการลดคุณภาพถาวรแบบเงียบๆ
ที่ไม่มีใครไปตรวจ เพราะห้องขายยังทำงานได้อยู่

**โมเดลที่เชื่อมต่อจริงคือ `self.model` ไม่ใช่ `settings.gemini_model`** เพราะ
http_options (v1beta), ฟิลด์ thinking และ warning เรื่อง affective dialogue
เลือกจากชื่อโมเดลทั้งหมด ถ้าอ่าน setting ตรงๆ session จะตั้งค่าให้โมเดลที่ไม่ได้รันอยู่

### คำไทยไม่มีช่องว่าง อย่าใช้ substring กับมันเด็ดขาด

**บั๊กนี้เกิดสองครั้งแล้วในโปรเจกต์เดียว** ครั้งแรกในการค้นสไลด์
(`ราคา` แมตช์ใน `อาคาร` เพราะใช้ `าคา` ร่วมกัน) ซึ่งเป็นเหตุผลที่รื้อชั้น retrieval ใหม่ทั้งหมด
ครั้งที่สองอยู่ห่างไปแค่ฟังก์ชันเดียว และ**แพงกว่า**

`_is_commercial` เทียบด้วย `term in text` ผลคือ
```
ยังไ*งบ*้าง   มีคำว่า งบ    -> "ที่ออกกำลังกายเป็นยังไงบ้าง" ถูกปฏิเสธ
*ผ่อน*คลาย    มีคำว่า ผ่อน  -> "ซาวน่าช่วยผ่อนคลายไหม" ถูกปฏิเสธ
```
"เป็นยังไงบ้าง" เป็นสำนวนไทยที่ธรรมดาที่สุดสำนวนหนึ่ง และ "ผ่อนคลาย" คือคำที่ลูกค้า
ใช้พูดถึงสปา ซึ่งโครงการนี้มีเต็มไปหมด **สไลด์ผิดคือรูปผิดรูปเดียว
แต่การปฏิเสธผิดคือหุ่นไม่ยอมตอบคำถามที่ตอบได้**

ตอนนี้คำไทยเทียบเป็น*คำ* (ผ่าน `tokenize`) ส่วนคำที่ไม่ใช่ไทยยังใช้ substring
เพราะภาษาละตินมีช่องว่าง และไม่มี segmenter สำหรับจีน/ญี่ปุ่นในนี้

**เจอเพราะทำให้ eval เดินผ่านด่านเดียวกับของจริง** ก่อนหน้านั้นมันข้ามด่านนี้มาตลอด

### วัดให้ตรงชั้นที่ลูกค้าเจอ

**เกิดสามครั้งในโปรเจกต์นี้แล้ว** การวัดที่อยู่ห่างจากสิ่งที่ตัดสินไปหนึ่งชั้น
อ่านแล้วดูเข้มงวด แต่ให้ตัวเลขของอย่างอื่น

1. eval ดู `hits[0]` ส่วน `search_condo_info` สแกนทุก hit — eval บอกว่าปฏิเสธ
   ขยะครบ ขณะที่เทสต์ยังแดง ทั้งคู่ถูกตามที่ตัวเองวัด
2. threshold วัดจากคำถามชุดหนึ่ง แล้วเอาไปยืนยันกับอีกชุดหนึ่ง
3. eval เรียก `search_slides` ตรงๆ ข้าม `_is_commercial()` ที่ของจริงดักก่อน —
   รายงานว่า "ค่าส่วนกลางเท่าไหร่" ตอบผิด ทั้งที่หุ่นไม่เคยตอบข้อนั้นด้วยสไลด์เลย

**threshold แก้ปัญหานี้ไม่ได้แล้ว วัดจบแล้วสองรอบ** ชุด 126 คำถาม:
คำถามจริงต่ำสุด 0.622 / คำถามขยะสูงสุด 0.700 ทับกันสนิท
คู่ที่ปฏิเสธขยะครบจะทิ้งคำถามจริง 47 จาก 90 ข้อ และทำได้เฉพาะโดยปิดสัญญาณข้างหนึ่ง
**ทางแก้ที่เหลืออย่างเดียวคือเติม keyword ให้สไลด์**
(วัดใหม่ 2026-08-22: ตอนนี้เติมไปเยอะแล้ว keyword เฉลี่ย 10.3 คำ เหลือ 9 หน้าที่ต่ำกว่า 3 คำ
และข้อความที่ search เห็นจริงเฉลี่ย 1,392 ตัวอักษร — ตัวเลข "น้อยกว่า 3 คำทุกหน้า / 127 ตัวอักษร"
ที่เคยเขียนไว้ตรงนี้เป็นสภาพก่อนเติม **ยังไม่ได้รัน `eval_search.py` ใหม่หลังเติม**
จึงยังไม่รู้ว่าช่องว่างที่ทับกันสนิทแยกออกหรือยัง)

**`eval_search.py` แคช query embedding ไว้** เพราะ 126 คำถามติดกันเกินเพดาน
embed_content ของ free tier (100 ครั้ง/นาที) รอบแรกชน 429 กลางทาง แล้วรันต่อ
โดยปิด semantic — คำถามที่เหลือได้ sim -1.00 แล้วถูกคัดออกจากการคำนวณ
ค่าที่พิมพ์ออกมาจึงคำนวณจากคำถามส่วนหนึ่ง และดูน่าเชื่อถือเท่ากับที่ครบ

### log ของลูกค้า

**`data/logs/` คือคำพูดของคนแปลกหน้าที่ไม่เคยถูกถาม** และไม่มีอะไรลบมันเลย
การเก็บไว้ตลอดไปจึงไม่เคยถูก*ตัดสินใจ* มันคือสิ่งที่เกิดขึ้นเมื่อไม่มีโค้ดบอกเป็นอย่างอื่น
ซึ่งเป็นวิธีที่แย่ที่สุดที่นโยบายเก็บข้อมูลจะเกิดขึ้นมา ตอนนี้ `TURN_LOG_KEEP_DAYS=30`

**แต่คุณค่าในการวิเคราะห์ไม่ได้อยู่ที่ข้อความดิบ** เกือบทุกคำถามที่ควรถาม log นี้
(ลูกค้าถามอะไร ตอบไม่ได้เรื่องไหน ภาษาไหนโผล่บ้าง สไลด์ขาดอะไร) ตอบได้ด้วยตัวเลข
`analyze_log.py --save` เขียนตัวเลขลง `data/log-summaries/` ซึ่งอยู่รอดพ้นการลบ
และ**จงใจไม่มี free text เลย** — ไม่มี heard ไม่มี said ไม่มีข้อความคำค้น
สรุปที่แอบพกคำถามดิบไปด้วยคือกล่องเก็บเดิมในชื่อใหม่ และเป็นเวอร์ชันที่ไม่มีใครนึกถึงว่าต้องตรวจ

**อายุนับจากชื่อไฟล์ ไม่ใช่ mtime** การก๊อปโฟลเดอร์ กู้ backup หรือโปรแกรม sync
แตะไฟล์ ล้วนเขียน mtime ใหม่ ไม่มีอันไหนเป็นเหตุผลให้เริ่มนับเวลาเก็บใหม่

**เอาไปเทรนโมเดลเป็นอีกเรื่อง** ไม่ใช่เรื่องเทคนิค — เป็นเรื่องฐานทางกฎหมายตาม PDPA
และในนั้นมีเบอร์โทรจริง เพราะ prompt สั่งให้หุ่นทวนเบอร์กลับเพื่อยืนยัน
ถ้าจะทำต้องคุยกับคนที่รับผิดชอบด้านนี้ก่อน ไม่ใช่ตัดสินในโค้ด

### ภาษาไทยกับการค้นหา

**ภาษาไทยไม่มีช่องว่างระหว่างคำ** ใช้ pythainlp newmm tokenize
character n-gram ทำให้ `ราคา` แมตช์ `อาคาร` (มี `าคา` ร่วมกัน) — เคยเกิดจริง

**`\w` ตัดวรรณยุกต์และสระออก** ทำให้ภาษาไทย/ฮินดีพัง ใช้ Unicode category (เก็บ L/M/N)

**`gemini-embedding-001` ต้อง normalize L2 เอง** ที่ 768 มิติ

**ห้ามผสม embedding สองสเปซเด็ดขาด** `EMBED_PROVIDER` เลือกได้ auto/gemini/local/off
เวกเตอร์จากคนละโมเดล**เทียบกันไม่ได้** — cosine ระหว่าง Gemini กับ MiniLM ไม่ error
มันคืนตัวเลขออกมา แล้วจัดอันดับสไลด์แบบสุ่ม ถาม "ฟิตเนส" ได้รูปผู้บริหาร โดยไม่มี log บอก
เลยเอา provider ไปผูกกับ fingerprint ของ cache — เปลี่ยน provider = index stale ทันที
และถ้า API ล่มกลางคัน ต้อง build ใหม่**ทั้งเด็ค** ด้วย local ห้ามต่อครึ่งเดียว

**embeddings ไม่เคยสร้างได้เพราะ rate limit ไม่ใช่บั๊กโค้ด** free tier จำกัด 100 req/นาที
ทุกครั้งที่รีสตาร์ตโดยไม่มี `embeddings.npz` มันจะ embed ใหม่หมด วันหนึ่งรีสตาร์ตหลายรอบ
peak ขึ้นไป 96/100 → พอชนก็ raise → log ว่า "lexical only" → **ไฟล์ไม่ถูกเขียน** →
รีสตาร์ตครั้งหน้าเริ่มใหม่ วนไม่จบ (RPD แค่ 160/1000 — ที่ชนคือลิมิตต่อ*นาที* ซึ่งรอได้)
ตอน build ใส่ `attempts=6` + backoff ถึง 60 วิ แต่ **ตอน query ต้องเป็น 1**
เพราะมันอยู่ใน tool call ที่มีเพดาน 15 วิ — รอที่นั่น = ลูกค้าได้ความเงียบแล้ว timeout

---

## วิธีทำงานที่เจ้าของโปรเจกต์คาดหวัง

**วัดก่อนเชื่อ** หลายครั้งที่การวัดขัดกับแผนแล้วต้องเปลี่ยนทาง — การ import
transcript ของแต่ละหน้าที่คิดว่าจะช่วยมาก วัดได้ +1 จาก 32 และ transcript ทำให้แย่ลง
สมมติฐานที่ไม่ได้วัดคือหนี้

**เช็คไฟล์จริงก่อนสรุป** เขาเคยสั่งตรงๆ ว่า *"ก่อนจะสรุปให้เช็คกับไฟล์จริงที่ทำด้วย"*

**ย้อนโค้ดกลับเพื่อพิสูจน์ว่าเทสต์แดงจริง** เคยมีเทสต์ที่ผ่านทั้งที่โค้ดถูก revert แล้ว
เพราะมันเช็ค side effect แทนที่จะเช็คตัวเงื่อนไข

**เขียนเหตุผลไว้ในโค้ด ไม่ใช่แค่ว่าโค้ดทำอะไร** คอมเมนต์ในไฟล์เหล่านี้บันทึกว่า
บั๊กไหนทำให้เกิดบรรทัดนี้ นั่นคือสิ่งที่กันไม่ให้ใครแก้กลับไปแบบเดิม

**อ่าน `data/logs/` ก่อนเดา** ตั้งแต่นี้ทุกเทิร์นถูกบันทึก: `heard` (ได้ยินว่าอะไร),
`said`, `tool`, `screen` (เปลี่ยนภาพตอน audio lead เท่าไหร่), `lookup` (ค้นแล้วเลือกอะไร
ทำไมไม่เลือกอันอื่น), `commercial_question` — คำถามแบบ "ทำไมสไลด์ไม่ตรง" ตอบได้จากไฟล์นี้

**ระวังการเอาคำแนะนำทั่วไปมาทาบกับปัญหาที่มีคำอธิบายอยู่แล้ว** เคยพลาดมาแล้ว:
อ้างตัวเลข "S2S เหมาะกับเครื่องมือ 1-3 ตัว" จากบทความ แล้วสรุปว่าเครื่องมือ 10 ตัว
คือสาเหตุ ทั้งที่อาการทุกอย่างที่เจอมีสาเหตุจริงที่หาเจอและแก้ไปแล้ว — คำสั่งวางผิดที่,
ตัวกระตุ้นป้อนตัวเอง, นาฬิกาสองเรือน ตอนนี้มี `analyze_log.py` ไว้ตอบด้วยข้อมูลแทน

**บั๊กถัดไปมักเกิดจากกลไกที่ใช้แก้บั๊กก่อนหน้า** เกิดซ้ำหลายรอบในโปรเจกต์นี้
ทุกครั้งที่เพิ่มกลไกใหม่ ให้ถามว่ามันจะไปชนกับอะไรที่มีอยู่

---

## งานที่ยังค้าง

- `data/condo_facts.json`: ราคาเริ่มต้น แบบห้อง/ขนาด โปรโมชั่น เวลาทำการ — **รอเจ้าของกรอก**
  พร้อม `approved_by` ที่ยังว่าง (prompt จะเตือนตัวเองว่ายังไม่อนุมัติจนกว่าจะกรอก)
- สคริปต์ 59 หน้ายังเป็น draft ต้องให้ฝ่ายขายอนุมัติ
- `data/slides/embeddings.npz` ยังไม่ได้สร้าง
- `SEARCH_MIN_SIMILARITY` / `SEARCH_SHOW_SIMILARITY` (0.45/0.60) ตั้งไว้เฉยๆ ยังไม่เคยวัด
- แยกประเภทเครื่องมือใน registry (ย้อนกลับได้ vs สั่งอุปกรณ์จริง) — **เจ้าของสั่งพักไว้ก่อน**
  เพราะการบังคับยืนยันจะชนกับกฎข้อ 8 ("เรียกเครื่องมือทันทีโดยไม่ต้องถามซ้ำ")
- Android Robot Bridge สำหรับฮาร์ดแวร์ Aobo
