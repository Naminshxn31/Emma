# Embassy World — หุ่นยนต์ต้อนรับห้องขาย

สรุปสถานะ ณ 6 สิงหาคม 2026 · ตรวจสอบกับไฟล์จริงในโฟลเดอร์นี้แล้ว

---

## 1. สถานะตอนนี้

| | |
|---|---|
| โค้ดหลัก | 3,572 บรรทัด (`app/`) |
| หน้าเว็บ | 889 บรรทัด (`client/`) |
| สคริปต์ | 572 บรรทัด (`scripts/`) |
| เทสต์ | 2,691 บรรทัด — **244 ผ่าน** |
| สไลด์ | 144 หน้า (122 MB) |

**พร้อมใช้:** คุยโต้ตอบด้วยเสียงต่อเนื่อง (ไม่ต้องกดปุ่ม), พูดแทรกได้, หลายภาษา, แสดงสไลด์ตามคำถาม, พรีเซนต์เป็นชุด, เปิด-ปิดไฟและแอร์, จอเต็มแยกหน้าต่าง

**ยังไม่พร้อม:** ข้อมูลราคา/โปรโมชั่นทั้งหมด (ดูข้อ 5)

---

## 2. สถาปัตยกรรม

```
ไมค์ ─▶ เบราว์เซอร์ ─WebSocket─▶ FastAPI ─▶ Gemini Live API
                 ◀── เสียงตอบ ──          (speech-to-speech)
                                    │
                                    ├─▶ เครื่องมือ: ไฟ, แอร์, สไลด์, ค้นข้อมูล
                                    └─▶ /display  จอเต็ม
```

**ตัดสินใจสำคัญ: ใช้ speech-to-speech ไม่ใช่ STT→LLM→TTS**
โมเดลฟังเสียงลูกค้าโดยตรงและพูดตอบเอง ไม่มีขั้นแปลงเป็นข้อความคั่นกลาง — เร็วกว่า และน้ำเสียงเป็นธรรมชาติกว่ามาก **ข้อความบนจอเป็นเพียงคำบรรยาย โมเดลไม่ได้อ่านมัน** (จุดนี้อธิบายบั๊กหลายตัวในข้อ 4)

**เปลี่ยนได้ 2 ผู้ให้บริการ** ผ่าน `VOICE_PROVIDER` — Gemini (ฟรี, ใช้อยู่) หรือ OpenAI Realtime (จ่ายเงิน) โค้ดฝั่งเบราว์เซอร์ไม่ต้องรู้ว่าใช้ตัวไหน

### ค่าที่ตั้งอยู่ตอนนี้

| | |
|---|---|
| โมเดล | `gemini-3.1-flash-live-preview` |
| เสียง | ตั้งใน `.env` · ชื่อหุ่น `Emma` |
| ภาษาที่ตอบ | `auto` — ตามภาษาลูกค้า |
| ภาษาคำบรรยาย | `th-TH,en-US,zh-CN` |
| ค้นหาเชิงความหมาย | เปิด (0.45 / 0.60) |
| ยุบช่องว่างไทย | ปิด |

---

## 3. สิ่งที่ทำไปทั้งหมด

### เสียงและการสนทนา
- คุยต่อเนื่องไม่ต้องกดปุ่ม พูดแทรกกลางประโยคได้
- กล่องสนทนาแสดงข้อความสองฝั่งแบบเรียลไทม์
- ตัวชี้วัดระดับเสียงไมค์ทันที ไม่ต้องรอ transcript
- รองรับ ~97 ภาษา สลับกลางบทสนทนาได้เอง
- กรอง emotion marker ออกจากข้อความ (มาจาก affective dialog)
- **ข้อความที่ยังไม่ได้พูดจะจางและขีดฆ่า** เมื่อถูกพูดแทรก

### สไลด์
- นำเข้า PDF → 144 สไลด์ พร้อมชื่อ/สรุป/คีย์เวิร์ด ไทย-อังกฤษ
- ตัดลายน้ำ "PRELIMINARY CONCEPT" ออกจากชื่ออัตโนมัติ
- แสดงตามคำถาม + พรีเซนต์เป็นชุด (โมเดลเรียก `next_slide` เอง)
- จอเต็มแยกหน้าต่าง `/display` เชื่อมสดต่ออัตโนมัติ
- แสดงในหน้าต่างแชทด้วย (แชท 480px ซ้าย, สไลด์กินที่เหลือ)
- รองรับสคริปต์บรรยายรายหน้า (`scripts/add_narration.py`)

### ค้นหา — เขียนใหม่ทั้งหมดเป็น hybrid search
| ชั้น | ทำอะไร |
|---|---|
| pythainlp | ตัดคำไทยจริง + เรียนคำศัพท์จากคีย์เวิร์ดสไลด์เอง |
| BM25 | ให้คะแนนคำ · ใช้**ยืนยัน**ว่าเจอจริง |
| Gemini embeddings | ความหมาย + ข้ามภาษา · ใช้**ค้นหา** |
| RRF | รวมสองอันดับ |

ล้มได้ทุกชั้นโดยระบบยังเดิน: ไม่มี pythainlp → n-gram · ไม่มี numpy/key/เน็ต → BM25 อย่างเดียว

### เครื่องมือ
- เปิด-ปิด-ปรับ ไฟและแอร์ ผ่าน Broadlink IR
- ระบบ registry เพิ่มฟังก์ชันใหม่ได้โดยไม่แตะโค้ดหลัก
- รายงานตรงไปตรงมาเมื่อสั่งอุปกรณ์ไม่สำเร็จ หรือยังไม่ได้ต่อของจริง

### เครื่องมือวัดผล
- `scripts/eval_search.py` — 30 คำถาม 9 ภาษา รวมคำถามที่ต้องปฏิเสธ **คำนวณค่า threshold ที่ควรตั้งจากเด็คของคุณเอง**

---

## 4. บั๊กที่แก้ (เฉพาะที่มีรากลึก)

### `ราคา` แสดงรูป `อาคาร`
ตัวค้นหาเดิมเทียบ**ตัวอักษร** (n-gram) เพราะไทยไม่มีช่องว่าง — แต่ `ราคา` กับ `อาคาร` ใช้ `าคา` ร่วมกัน ระบบอ่านว่าเหมือนกัน 50%
→ แก้ด้วยการตัดคำจริง `ราคาโครงการ` → `["ราคา","โครงการ"]` ปัญหาหายทั้งคลาส

### `ao rummy` / `ไอ้บ้า`
คำบรรยายผิดภาษาสองทิศทาง — เสียงไทยถูกถอดเป็นอังกฤษ แล้วพอตั้งให้ไทยนำ "ice bath" กลับกลายเป็นคำหยาบ **แล้วโมเดลขอโทษว่าทำให้ไม่พอใจ**
→ ฟิลด์ที่ใช้อยู่ deprecated ทั้งหมด (`language_hints`, `adaptation_phrases`) เปลี่ยนเป็น `language_codes` + `custom_vocabulary`, ป้อนชื่อสถานที่ 27 ชื่อจากสไลด์เอง, และเพิ่มกฎ **"ได้ยินคำหยาบ = ฟังผิดเสมอ ห้ามขอโทษว่าทำให้ไม่พอใจ"**

### หุ่นแนะนำมือถือ Xiaomi กับโน้ตบุ๊ก Acer
กฎห้ามคุยนอกเรื่องอยู่**บรรทัดสุดท้าย**ของ prompt และยกตัวอย่างแต่*บริการ* การแนะนำ*สินค้า*จึงไม่เข้าข่าย
→ ย้ายขึ้นเป็นกฎข้อแรก เขียนใหม่ให้ครอบคลุม + `แม้จะรู้คำตอบ` + `ห้ามตอบให้แม้บางส่วน`

### ทุกภาษาที่ไม่ใช่ไทย/อังกฤษถูกทิ้งเงียบๆ
ตัวแยกคำที่ผมเขียนคือ `[A-Za-z0-9]+|[฀-๿]+` — จีน รัสเซีย ญี่ปุ่น เกาหลี อาหรับ ฮินดี ได้ token เป็นศูนย์ **และอันดับที่ว่างเปล่านั้นยังได้โหวต 40%**
→ ตัดตาม Unicode category · CJK ใช้ bigram · ถ้าไม่มีสัญญาณก็ไม่มีสิทธิ์โหวต

### หุ่นพูดว่า "No module named 'numpy'" ใส่ลูกค้า
`import numpy` อยู่**นอก** try ที่เขียนไว้ดักเรื่องนี้โดยเฉพาะ exception หลุดไปถึงคำตอบ
→ เช็คครั้งเดียวตอนเริ่ม เตือนใน log แล้วปิด semantic

### อื่นๆ
เซสชันตายหลังตอบครั้งเดียว · เสียงซ้อนกัน · ไม่มีเสียงหลังกด Start ใหม่ · OpenAI ตอบเหมือนผู้ช่วยทั่วไป (session config ถูกปฏิเสธเงียบๆ) · ข้อความขาดกลางประโยค · ด่านกันเรื่องราคาพูดได้แค่ไทย-อังกฤษ

---

## 5. ยังค้าง — รอข้อมูลจากคุณ

### `CONDO_FACTS` ว่างทั้ง 6 ช่อง ← สำคัญที่สุด
`app/prompts.py` — ทำเล, ราคาเริ่มต้น, แบบห้อง, สิ่งอำนวยความสะดวก, โปรโมชั่น, เวลาทำการ

ตอนนี้ถ้าลูกค้าถามราคา หุ่นจะบอกว่า *"ยังไม่มีข้อมูล ติดต่อฝ่ายขายค่ะ"* — ถูกตามกฎ (ห้ามแต่งตัวเลข) แต่ตอบอะไรไม่ได้เลย **และราคาคือคำถามแรกของลูกค้าจริงทุกคน**

### สคริปต์บรรยาย 59 หน้า
ยังไม่มีสักหน้า (`scripts/add_narration.py` พร้อมรับแล้ว) — ตอนนี้โมเดลบรรยายเอง ซึ่งเสี่ยงพูดนอกบทที่อนุมัติ

### สไลด์ 13 หน้าไม่มีชื่อ
ชื่อเป็น "สไลด์ที่ N" — ค้นหาไม่เจอ เจอได้เฉพาะตอนไล่ดูตามลำดับ

### ยังไม่ได้สร้าง embedding
`data/slides/embeddings.npz` ยังไม่มี รันครั้งแรกจะสร้างเอง หรือสั่ง:
```
python scripts/eval_search.py
```

---

## 6. ข้อจำกัดที่ควรรู้

**ค่า threshold ยังไม่เคยวัด** — `0.45` / `0.60` เป็นค่าเดา ไม่เคยทดสอบกับเด็คจริง เพราะผมเรียก API ด้วย key ของคุณไม่ได้ `eval_search.py` จะบอกค่าที่ควรใช้

**ปรับให้ผิดไปทางไม่เปลี่ยนรูป** — บางครั้งตอบถูกแต่ไม่เปลี่ยนภาพให้ ดีกว่าเอารูปผิดขึ้นจอแล้วขัดกับสิ่งที่พูด

**จุดตัดข้อความที่ถูกพูดแทรกเป็นการประมาณ** — คลาดได้สองสามคำ API ไม่ได้บอกว่าหยุดที่คำไหน

**หนี้ที่ยังไม่ได้จ่าย** — stopwords มีแค่ไทย/อังกฤษ · ป้ายกำกับหน้าเว็บตายตัว · ยังไม่ได้ต่อ Canva API (คุณตกลงเลื่อนไว้)

**ยังไม่ได้ทดสอบบนหุ่นจริง** — ทั้งหมดรันบนพีซี ยังไม่ได้ต่อกับ Aobo SDK
